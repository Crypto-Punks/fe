const initialState = {
  netWorth: 100000.00,
  investedCoins: [],
  watchList: [],
  top100: [],
};

export default function reducer(state = initialState, action) {
  switch(action.type) {
    default:
      return state;
  }
}
