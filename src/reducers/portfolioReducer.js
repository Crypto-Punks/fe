

const initialState = {
  netWorth: 100000.00,
  investedCoins: [],
  watchList: []
};

export default function reducer(state = initialState, action) {
  switch(action.type) {
    default:
      return state;
  }
}
